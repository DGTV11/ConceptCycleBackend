services:
  cc-backend:
    build: .
    # develop:
      # watch:
        # - action: rebuild
        #   path: ./pyproject.toml
    volumes:
      # - .:/app:rw
      # - /app/.venv
      - ./.env:/app/.env
      - ./db.sqlite:/app/db.sqlite
    ports: 
      - "5046:5046"
